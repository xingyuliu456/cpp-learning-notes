# 1. 忽略根目录下的所有文件和文件夹
/*

# 2. 允许上传 .gitignore 文件本身（方便以后修改规则）
!.gitignore

# 3. 允许上传你指定的两个核心文件夹
!/01类和对象/
!/02内存管理/

# 4. 在允许的文件夹内部，进一步过滤掉垃圾文件
# 过滤掉所有 .exe 编译程序（不管在哪个层级）
*.exe
# 过滤掉 VS Code 自动生成的配置文件夹
.vscode/
# 过滤掉一些常见的 C++ 临时文件
*.o
*.obj